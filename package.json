{
  "name": "bible-viewer-test",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npm run copy-files && vite build",
    "preview": "vite preview",
    "copy-files": "copy robots.txt dist && copy sitemap.xml dist"
  },
  "dependencies": {
    "@popperjs/core": "^2.11.8",
    "less": "^4.2.0",
    "vue": "^3.4.21"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^5.0.4",
    "copy": "^0.1.3",
    "vite": "^5.2.0"
  }
}
